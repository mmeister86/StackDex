{
  "name": "stackdex-convex",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run convex:test",
    "convex:test": "vitest run",
    "convex:test:watch": "vitest",
    "convex:dev": "convex dev",
    "convex:deploy": "convex deploy"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "convex": "^1.23.0",
    "convex-test": "^0.0.20",
    "typescript": "^5.8.3",
    "vitest": "^3.1.1"
  }
}
